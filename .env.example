# Copia este archivo como .env y rellena los valores (NUNCA subas .env a Git)
DB_NAME=wordpress
DB_USER=wpuser
DB_PASSWORD=cambia_esto
DB_ROOT_PASSWORD=cambia_esto_root

# URL local para el search-replace tras importar el dump de producción
LOCAL_URL=http://localhost:8080
PROD_URL=https://colibridge.es
